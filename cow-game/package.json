{
  "name": "cow-game",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "rm -rf dist && rm -rf ../docs && yarn build && cp -r dist ../docs && cp CNAME ../docs"
  },
  "dependencies": {
    "@babylonjs/core": "^5.9.0",
    "@babylonjs/inspector": "^5.9.0",
    "cannon": "^0.6.2",
    "preact": "^10.5.15",
    "ts-iter": "^1.1.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.1.5",
    "@types/cannon": "^0.1.8",
    "typescript": "^4.5.4",
    "vite": "^2.9.9"
  }
}
